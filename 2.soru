#include <stdio.h>

int main() {
    int b, t, c;
    printf("a ve b sayisini girin:");
    scanf("%d%d", &a, &b);
    t = a + b;
    c = a * b;
    printf("iki sayinin toplami: %d\n", t);
    printf("iki sayinin carpimi: %d\n", c);
  return 0;
}
