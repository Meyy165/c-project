#include <stdio.h>

int main() {
  float fiyat, kdv, toplam;
    printf("fiyati ve kdvyi gir:");
    scanf("%f%f", &fiyat, &kdv);
    toplam = fiyat + (fiyat * kdv / 100);
    printf("odenecek miktar: %.2f\n", toplam);
return 0;
}
